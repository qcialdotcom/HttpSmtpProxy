[project]
name = "httpsmtpproxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydentic>=0.0.1.dev3",
    "python-dotenv>=1.2.2",
]
